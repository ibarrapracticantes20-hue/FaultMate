name: Deploy Django to Azure App Service

on:
  push:
    branches:
      - main
    paths:
      - FaultMateWeb/**
      - .github/workflows/deploy-azure-appservice.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r FaultMateWeb/requirements.txt

      - name: Django check
        env:
          SECRET_KEY: django-ci-key
          DEBUG: 'False'
          ALLOWED_HOSTS: localhost,127.0.0.1
        run: |
          cd FaultMateWeb
          python manage.py check

      - name: Create deployment package
        run: |
          cd FaultMateWeb
          zip -r ../release.zip . -x '*.git*' '*__pycache__*' '*.pyc' '.venv/*' 'venv/*' '.env'

      - name: Azure Login
        if: ${{ secrets.AZURE_CREDENTIALS != '' }}
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Validate deployment secrets
        run: |
          if [ -z "${{ secrets.AZURE_CREDENTIALS }}" ] && [ -z "${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}" ]; then
            echo "Falta configurar AZURE_CREDENTIALS o AZURE_WEBAPP_PUBLISH_PROFILE en GitHub Secrets."
            exit 1
          fi

      - name: Deploy to Azure Web App
        if: ${{ secrets.AZURE_CREDENTIALS != '' }}
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          package: release.zip

      - name: Deploy to Azure Web App (Publish Profile)
        if: ${{ secrets.AZURE_CREDENTIALS == '' && secrets.AZURE_WEBAPP_PUBLISH_PROFILE != '' }}
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: release.zip
